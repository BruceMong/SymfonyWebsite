{% extends 'base.html.twig' %}

{% block title %}Réinitialisation mot de passe Email envoyé {% endblock %}
{% block stylesheets %} {{ encore_entry_link_tags('app') }} {{ encore_entry_link_tags('login') }} {% endblock %} 

{% block body %}
<div class="login-page">
    <div class="form">
    <p>
    Si un compte possédant cette adresse mail existe, alors un mail contenant un lien pour changer votre mot de passe vous sera envoyé.
    <br/>Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle')|replace({"hour": "heure", "day": "jour"}) }}.
    </p>
    <p>Si vous ne recevez pas de mail regarder dans vos spams ou <a href="{{ path('app_forgot_password_request') }}">essayer encore</a>.</p>
    </div>
    </div>
{% endblock %}
